import { TimeConstraintDirective } from './time-constraint.directive';

describe('TimeConstraintDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeConstraintDirective();
    expect(directive).toBeTruthy();
  });
});
